<resources xmlns:tools="http://schemas.android.com/tools">
    <dimen name="max_width">300dp</dimen>
    <dimen name="acc_size">16sp</dimen>
    <dimen name="acc_dist">25dp</dimen>

    <style name="Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/purple</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/purple</item>

    </style>

    <style name="BottomNavText" parent="TextAppearance.Material3.BodySmall">
        <item name="android:textSize">10sp</item>
    </style>

    <style name="MyBottomNavStyle" parent="Widget.Material3.BottomNavigationView">
        <item name="itemIconSize">22dp</item>
        <item name="itemTextAppearanceActive">@style/BottomNavText</item>
        <item name="itemTextAppearanceInactive">@style/BottomNavText</item>
    </style>

    <style name="Shape.Bubble.Out" parent="@style/ShapeAppearance.Material3.LargeComponent">
        <item name="cornerSize">16dp</item>
    </style>

    <style name="Shape.Bubble.In" parent="@style/ShapeAppearance.Material3.LargeComponent">
        <item name="cornerSize">16dp</item>
    </style>

    <style name="ToolbarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">25sp</item>
    </style>
</resources>